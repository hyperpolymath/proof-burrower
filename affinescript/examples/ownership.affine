// SPDX-License-Identifier: MPL-2.0
// Ownership and borrowing (parser-compatible example)

fn takes_owned(x: own String) -> () = ();

fn takes_ref(x: ref Int) -> Int = 42;

fn takes_mut(x: mut Bool) -> () = ();

struct File {
  fd: own Int
}

fn read_file(f: ref File) -> Int = 42;
